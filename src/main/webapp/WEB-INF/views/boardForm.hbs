<html>
<head>
    <title>게시글 업데이트</title>
</head>
<body>
<div class="main_container">
    {{#if boardDto}}
        <form method="post" action="/boards/{{boardDto.id}}">
            <h1>게시판 수정화면</h1>
            <div>
                <table>
                    <tr>
                        <th>제목</th>
                        <td><input type="text" name="title" value="{{boardDto.title}}"></td>
                    </tr>
                    <tr>
                        <th>작성자</th>
                        <td><input type="text" name="writer" value="{{boardDto.writer}}"></td>
                    </tr>
                    <tr>
                        <td colspan="2"><textarea name="content" value="{{boardDto.content}}">{{boardDto.content}}</textarea>
                        </td>
                    </tr>
                </table>
            </div>
            <div>
                <span><button><a href="/boards/{{boardDto.id}}">취소</a></button></span>
                <input type="hidden" name="_method" value="patch">
                <span><input type="submit" value="작성"></span><!-- boards/{id}에 PATCH로 -->
            </div>
        </form>
        
    {{else}}
        <form method="post" action="/boards">
            <h1>게시판 등록화면</h1>
            <div>
                <table>
                    <tr>
                        <th>제목</th>
                        <td><input type="text" name="title" placeholder="제목을 입력하세요." value="{{boardDto.title}}"></td>
                    </tr>
                    <tr>
                        <th>작성자</th>
                        <td><input type="text" name="writer" placeholder="작성자를 입력하세요." value="{{boardDto.writer}}"></td>
                    </tr>
                    <tr>
                        <td colspan="2"><textarea name="content" placeholder="내용을 입력하세요."
                                                  value="{{boardDto.content}}"></textarea></td>
                    </tr>
                </table>
            </div>
            <div>
                <span><button><a href="/boards">취소</a></button></span>
                <span><input type="submit" value="작성"></span><!-- board에 POST로 -->
            </div>
        </form>
    {{/if}}
</div>
</body>
</html>

<style type="text/css">
    body {
        background-color: #eaeaea;
    }

    .main_container {
        width: 1000px;
        margin: auto;
        background-color: white;
    }

    th {
        width: 100px;
    }

    table {
        width: 900px;
        margin: auto;
    }

    textarea[name="content"] {
        width: 100%;
        height: 500px;
        resize: none;
    }

    input[type="text"] {
        width: 100%;
    }

    a {
        text-decoration: none;
        color: black;
    }
</style>

